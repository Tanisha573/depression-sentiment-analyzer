services:
  - type: web
    name: depression-sentiment-analyzer
    env: python
    buildCommand: "pip install -r requirements.txt && python -c \"import nltk; nltk.download('punkt'); nltk.download('punkt_tab')\""
    startCommand: "gunicorn app:app"